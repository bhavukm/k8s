apiVersion: batch/v1
kind: Job
metadata:
  name: job3
spec:
  completions: 2
  template:
    spec:
      containers:
      - name: busybox
        image: busybox
        command: ["sleep", "60"]
      restartPolicy: Never
